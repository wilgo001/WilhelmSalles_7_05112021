class SuggestionManager {

    addSuggestions(recipe) {
        let ingredients = recipe.ingredients;
        for(let i = 0; i < ingredients.length; i++) {
            let ing = ingredients[i].ingredient;
            
        }
    }
}